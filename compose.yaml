name: mori

services:
  postgres:
    image: postgres:17.8-bookworm
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-mori}
      POSTGRES_USER: ${POSTGRES_USER:-mori}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mori_local_only}
      POSTGRES_INITDB_ARGS: --data-checksums
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - mori_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 2s
      timeout: 3s
      retries: 15
      start_period: 5s
    stop_grace_period: 30s

volumes:
  mori_postgres_data:
